# Supabase Configuration for ClassPass

# Supabase Project Settings (get these from supabase.com dashboard)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Development Settings
NODE_ENV=development

# Production Settings
# NODE_ENV=production

# Instructions:
# 1. Copy this file to .env
# 2. Replace the URL and key with your actual Supabase project details
# 3. Get these from: Supabase Dashboard → Settings → API
